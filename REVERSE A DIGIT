#include <stdio.h>
int n,i,rev,temp;
int main()
{
printf("Enter a number");
scanf("%d",&n);
for(i=1;n>0;i++)
{
    rev=n%10;
    n=n/10;
    temp =temp*10+rev;
}
printf("the rev digits is %d",temp);
return(0);
}
